server:
  port: 8762

eureka:
  instance:
    prefer-ip-address: false  #客户端注册时用自己ip,不是主机名
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: server-user